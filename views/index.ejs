<%- include('layout', { title: '首页', isAdmin: false }) %>

<div class="grid">
  <div class="card">
    <h3>Clash 订阅</h3>
    <div class="copy-box">
      <input type="text" value="<%= clashUrl %>" readonly style="flex:1">
      <button onclick="copyToClipboard('<%= clashUrl %>')" class="secondary">
        复制
      </button>
    </div>
  </div>

  <div class="card">
    <h3>V2Ray 订阅</h3>
    <div class="copy-box">
      <input type="text" value="<%= v2rayUrl %>" readonly style="flex:1">
      <button onclick="copyToClipboard('<%= v2rayUrl %>')" class="secondary">
        复制
      </button>
    </div>
  </div>
</div>
