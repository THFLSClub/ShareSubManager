<nav class="bg-white shadow-sm" x-data="{ isOpen: false }">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <div class="flex-shrink-0 flex items-center">
        <h1 class="text-2xl font-bold text-slate-800 tracking-tighter">
          <a href="/" class="hover:text-blue-600 transition-colors">SubsManager</a>
        </h1>
      </div>
      
      <div class="hidden sm:ml-6 sm:flex sm:items-center space-x-4">
        <% if (locals.isAdmin) { %>
          <a 
            href="/admin"
            class="px-3 py-2 rounded-md text-sm font-medium text-gray-700 hover:bg-gray-100 hover:text-gray-900"
          >
            节点管理
          </a>
          <a 
            href="/logout"
            class="px-3 py-2 rounded-md text-sm font-medium text-red-600 hover:bg-red-50"
          >
            登出
          </a>
        <% } else { %>
          <a 
            href="/login"
            class="px-3 py-2 rounded-md text-sm font-medium text-gray-700 hover:bg-gray-100 hover:text-gray-900"
          >
            管理员登录
          </a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
